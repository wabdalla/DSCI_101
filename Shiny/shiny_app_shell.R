library(shiny)
library(bslib)

# Define user interface 
ui <- page_sidebar(
  
)

# Define server logic
server <- function(input, output) {
  
}

# Run the app 
shinyApp(ui = ui, server = server)